{
  "name": "@lyx/sdk",
  "version": "0.1.0",
  "type": "module",
  "description": "Lyx SDK - Runtime tools for MFE loading, events, shared state, and navigation",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@lyx/types": "workspace:*",
    "zustand": "^5.0.0"
  },
  "peerDependencies": {
    "react": ">=18.0.0",
    "react-dom": ">=18.0.0"
  },
  "files": ["dist", "src"]
}
